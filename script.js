// Script untuk interaksi (opsional)
document.querySelectorAll('.service-button').forEach(button => {
    button.addEventListener('', () => {
        alert('wait!'); // Ganti dengan interaksi yang sesuai
    });
});